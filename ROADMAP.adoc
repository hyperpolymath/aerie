= Roadmap: Advanced Network Forensics Suite
:revnumber: 2.0
:revdate: 2026-09-23

== Phase 0: Spec & Governance Foundation

[x] Bottom-up K9 specs system with guarded outputs.
[x] GraphQL schema outline with module-scoped subscriptions.
[x] Proof envelope spec (light/full) for all responses.
[x] Resolver policy gate and subscription auth flow.
[x] Bitemporal storage plan (valid-time + tx-time).
[x] OSI forensics matrix and HUD widgets.

== Phase 1: Core Telemetry & Containment

[x] Finalise Oxford British English documentation standards.
[x] Select toolchain: LibreSpeed (Speed), Hyperglass (Path), SmokePing (Stability).
[x] Initial compose.yml defining the "Forensics Bridge" network (5 services: gateway, librespeed, smokeping, redis, hyperglass).
[x] Triple-mount API gateway (GraphQL + gRPC + REST) with policy gate, proof envelopes, and service clients.

=== Phase 1 Implementation Details

* **Gateway** (Zig, Idris2 ABI): `src/api/zig/main.zig` — HTTP on port 4000, gRPC on port 4001
* **Proof envelopes**: SHA-256 hash-based ("light" mode) on all responses
* **Policy gate**: Phase 1 permissive — all requests allowed, API keys validated and logged
* **Service clients**: LibreSpeed (telemetry), Hyperglass (BGP routes), Redis (cache + audit)
* **Container**: Chainguard wolfi-base (build) → static (runtime), statically linked binary
* **Protocols**: GraphQL (POST /graphql), REST (GET /api/v1/*), gRPC (length-prefixed binary)

== Phase 2: Historical Integrity & Forensics

[ ] Implement SmokePing "Smoke" charts to identify 24h jitter patterns.
[ ] Configure Hyperglass nodes for multi-region path auditing.
[ ] Setup persistent Docker volumes for forensic log retention.
[ ] Wire VerisimDB temporal modality for bitemporal audit.

== Phase 3: Hardening & Shielding

[ ] Deploy Cloudflare API Shield to protect the diagnostic endpoints.
[ ] Integrate Ed448-signed logging for tamper-proof audit trails.
[ ] Implement mTLS (Mutual TLS) for dashboard access.

== Phase 4: Intelligence Dashboard

[x] Customise Dashy theme with "Midnight Security" aesthetic (HUD spec).
[x] Automated alerting via Webhooks for packet loss > 5% (policy hooks defined).

== Phase 4.5: Alerting, Retention & Guardrails

[x] Document Dashy alert badges, SmokePing retention tiers, and VerisimDB audit hooks.
[x] Publish known limitations for relative binds, secrets, automation throttles, and regen procedures.

== Phase 5: Passive Protocol Forensics

[ ] Deploy Zeek or Suricata in listen-only mode.
[ ] Map L4 flow data to realtime heatmap.
[ ] Implement L7 SNI sniffing for encrypted destinations.
[ ] Build OSI-layered status bar for the HUD.

== Phase 6: Proposed Architectural Enhancements

=== PAN (Personal Area Network) Enhancements: The "Last Meter"

[ ] **Bluetooth & BLE Monitoring Module:** Integrate a Bluetooth scanner to discover and monitor nearby PAN devices (headsets, IoT sensors), showing signal strength and latency in a new dashboard widget.
[ ] **USB Device Auditing:** Add a module to inventory connected USB devices and their bus status, helping to identify faulty hardware causing network-related issues.
[ ] **Wi-Fi Direct / Ad-hoc Network Analysis:** A module to detect and analyze local peer-to-peer networks used by services like screen mirroring.

=== LAN (Local Area Network) Enhancements: Mastering the Local Environment

[ ] **Physical Layer Topology Mapping (LLDP/CDP):** Implement a listener for LLDP/CDP to create a dynamic, interactive map of the physical network topology (e.g., which server is on which switch port).
[ ] **DHCP & DNS Server Auditing:** A module to actively monitor the health and response times of local DHCP and DNS servers.
[ ] **UPnP/SSDP Monitoring:** A security-focused widget that listens for and logs all UPnP and SSDP broadcasts to identify insecure devices opening ports.

=== MAN (Metropolitan Area Network) & WAN (Wide Area Network) Enhancements: Beyond the Edge

[ ] **Distributed Probes / "Aerie Agents":** Develop lightweight, secure probes for remote deployment in cloud VPCs or remote offices, enabling a full mesh of performance testing.
[ ] **Internet Weather & Major Outage Aggregation:** Integrate with public internet outage dashboards (e.g., Cloudflare Radar) to cross-reference local issues with global events.
[ ] **IXP (Internet Exchange Point) Path Analysis:** Enhance BGP path analysis to specifically identify and flag when traffic traverses major public IXPs.

=== Holistic & Security Enhancements

[ ] **Historical Network "Time Machine":** Implement a "Time Travel" mode in the UI, allowing the user to view the entire dashboard's state at any specific point in the past.
[ ] **SCADA/OT Network Monitoring Mode:** A "Passive-Only" mode that disables all active probes to allow for safe deployment in sensitive industrial environments.
[ ] **Post-Quantum Cryptography for Agents:** Use PQC algorithms (e.g., Kyber/Dilithium) for securing communication between the central Aerie instance and its distributed probes.

== Phase 7: Repository Remediation & Perfection (2026-09-23 audit)

Deferred and perfective items from the comprehensive audit
(2026-09-23, branch `arena/01a0cf8a-aerie`). Each item: classification
(🟠 corrective / 🟡 adaptive /  perfective), estimated effort (S = <1h,
M = half-day, L = day+), and dependencies. Full evidence in
`docs/reports/quality/REMEDIATION-PLAN.md`; settings deltas in
`docs/REPO-SETTINGS.adoc`.

=== Formal proofs (protocol 4.3 — LEAN4, no prior assistant work)

* **P1 — Proof-envelope invariants.** 🟢 PERFECTIVE · L · P0.
  LEAN4 project (`verification/lean/`, lakefile + Mathlib) proving:
  envelope well-formedness is preserved by the hash step; the "light"
  envelope is injective in (payload, queryId) for fixed timestamp; hash
  length is exactly 256 bits. Warrants the Idris2 model suite's P-suite.
* **P2 — Policy-gate monotonicity.** 🟢 PERFECTIVE · M · P1 (project
  scaffolding). The count-based window gate: allowed ⟹ below limit;
  denied-rate-limited is monotone in the window count.
* **P3 — Route matching correctness.** 🟢 PERFECTIVE · M · P1.
  Longest-prefix match over the route table is well-defined and
  deterministic; the `/` catch-all is the unique fallback.
* **P4 — CI leg for proofs.** 🟠 CORRECTIVE · S · P1.
  Workflow step (or `just proofs`) that runs `lake build` on the LEAN4
  project so proof debt cannot silently rot (hypatia-rules
  `proof-freshness` applies).

=== Benchmarks (protocol 4.2 — proven-tests format)

* **B1 — Idris2 benchmark skeleton.** 🟡 ADAPTIVE · M · tests/idris2
  first green type-check. `benchmarks/Benchmark.idr` + ipkg in the
  estate format (median-of-N, frozen workloads, JSON output): route
  table matching, envelope formatting, gate evaluation.
* **B2 — Zig gateway micro-benchmarks.** 🟢 PERFECTIVE · M · CI green.
  A `bench` step in `build.zig` (proof-envelope hashing, resolver
  dispatch) with baselines recorded per workload.

=== Drift & architecture (owner decisions)

* **D1 — `src/api/rust/` removal or re-home.** ✅ DONE 2026-09-24 —
  twin removed with the root Cargo workspace, `rust-ci.yml`,
  `.gitlab-ci.yml` cargo stages, and mise/.tool-versions rust pins
  (owner decision; the aspect-weave branch).
* **D2 — `src/stale/hyperglass` disposition.** 🟠 CORRECTIVE · S ·
  owner decision. Delete (upstream has its own repo) or re-home the
  `.samples/` deployment fixtures that the BGP lab references.
* **D3 — `.machine_readable/6a2/` → `descriptiles/` spelling.** 🟡
  ADAPTIVE · S · deed stable. Template convention; low value, churn
  risk — do only alongside a descriptile content update.

=== Estate wiring

* **E1 — Guix build-side derivation.** 🟡 ADAPTIVE · M · P4 (proofs
  CI first, to keep the package honest). `define-public aerie` with a
  source origin + sha256 + zig build system, replacing the manifest
  once the build integration exists (language policy §2.1: no stubs).
* **E2 — boj-server cartridge endpoint.** 🟡 ADAPTIVE · S · provisioning
  of a real cartridge endpoint in `boj-server-cartridges`. The
  `boj-build.yml` trigger is dormant (localhost, continue-on-error) by
  design until then.
* **E3 — gossamer/burble registration.** 🟢 PERFECTIVE · S · E2
  pattern. aerie is a service repo; register it for estate service
  discovery (stub in `.machine_readable/integrations/`).
* **E4 — spline/cleave integration assessment.** 🟢 PERFECTIVE · S ·
  their repos reaching readiness.
* **E5 — `actions.lock` + optional workflows.** 🟢 PERFECTIVE · S ·
  actions-lock tooling. `.github/workflows/actions.lock` (SHA lock) and
  the catalog's optional set (main-estate-audit, rsr-compliance-canary,
  estate-rules, ossf-best-practices) after the core set is green.

=== Toolchain & verification (TRG first audit)

* **T1 — Idris2 CI leg.** 🟠 CORRECTIVE · S · tests/idris2 green once.
  A workflow (or `just test` step in rust-ci-style wrapper) that
  type-checks and runs `tests/idris2` — until then the model suite is
  scaffold-with-intent.
* **T2 — TRG first audit.** 🟡 ADAPTIVE · M · T1, T3. Grade each
  toolchain component (zig, rust-drift, idris2, julia, guix, podman)
  against `toolchain-readiness-grades/` with machine-checkable claims;
  replace the honest X in READINESS.adoc.
* **T3 — Toolchain digest pins.** 🟢 PERFECTIVE · S · T1. Pin base
  images by digest in Containerfile/.devcontainer for reproducible
  builds (the `:latest` comments in both files name this debt).

=== Documentation & wiki

* **W1 — Wiki scaffolding.** 🟢 PERFECTIVE · M · plan review. Create
  the Home + three audience pages (docs/wiki-plan.adoc level 0), then
  the Users pages; content as plain Markdown per the berrywiki ruling.
* **W2 — Hero/social preview image.** 🟢 PERFECTIVE · S · prompt review.
  Generate the 1280×640 hero (prompt in docs/REPO-SETTINGS.adoc §hero),
  commit under `assets/`, owner sets the social preview.
* **W3 — `docs/just-cookbook.adoc` generation in CI.** 🟢 PERFECTIVE · S
  · none. `just cookbook` step on push so the verb matrix stays live.

=== Cross-repository (protocol Phase 9)

* **X1 — proven-tests-and-benches contribution.** 🟠 CORRECTIVE · S ·
  T1 (suite green first). Contribute `tests/idris2` as a corpus entry
  (its `depends.a2ml`/`diagnosticity.a2ml` are already in the corpus
  format); owner push.
* **X2 — Gists.** 🟢 PERFECTIVE · S · none. Reusable artefacts from this
  audit (repo-deed scaffold, REPO-SETTINGS measurement pattern) as
  gists — owner preference (public for others, private for owner).
* **X3 — Star taxonomy.** 🟡 ADAPTIVE · S · owner access. Star aerie;
  create the third-party star list; move third-party stars into it.
